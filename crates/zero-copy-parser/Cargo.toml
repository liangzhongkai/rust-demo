[package]
name = "zero-copy-parser"
version = "0.1.0"
edition = "2021"

[dependencies]
bytes = "1.7"
memchr = "2.7"
memmap2 = "0.9"

# 用于对比
serde = { version = "1.0", features = ["derive"] }
serde_json = "1.0"
